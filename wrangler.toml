name = "cytrack-ai"
main = "cytrack-gemini-worker.js"
compatibility_date = "2024-01-01"

[vars]
# Leave APP_SECRET empty here — we'll set it in the dashboard as a secret
# Just declaring it so the worker knows it exists
